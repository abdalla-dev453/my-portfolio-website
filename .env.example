# A Formspree, Resend/serverless, or other HTTPS endpoint that accepts JSON contact submissions.
# Leave unset in local development to use the email-client fallback.
VITE_CONTACT_FORM_ENDPOINT=https://your-form-endpoint.example/contact

import { useEffect, useState } from 'react';
import { Menu, X } from 'lucide-react';
const navLinks = [{ label: 'About', href: '#about' }, { label: 'Work', href: '#projects' }, { label: 'Experience', href: '#experience' }, { label: 'Contact', href: '#contact' }];
export default function Navbar() { const [open, setOpen] = useState(false); const [scrolled, setScrolled] = useState(false); useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 20); window.addEventListener('scroll', onScroll, { passive: true }); return () => window.removeEventListener('scroll', onScroll); }, []); const close = () => setOpen(false); return <header className={`fixed inset-x-0 top-0 z-40 border-b transition-colors ${scrolled ? 'border-line bg-ink/95' : 'border-transparent bg-ink/70'}`}><nav className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6 sm:px-8 lg:px-12"><a href="#home" className="font-display text-2xl text-paper" onClick={close}>Msema<span className="text-accent">.</span></a><div className="hidden items-center gap-7 md:flex">{navLinks.map((link) => <a key={link.href} href={link.href} className="text-[10px] font-bold uppercase tracking-[0.16em] text-stone-400 transition-colors hover:text-paper">{link.label}</a>)}</div><a href="#contact" className="hidden text-[10px] font-bold uppercase tracking-[0.16em] text-accent md:block">Let’s talk ↗</a><button type="button" aria-label="Toggle navigation" onClick={() => setOpen((value) => !value)} className="text-paper md:hidden">{open ? <X /> : <Menu />}</button></nav>{open && <div className="border-t border-line bg-ink px-6 py-5 md:hidden">{navLinks.map((link) => <a key={link.href} href={link.href} onClick={close} className="block py-3 text-xs font-bold uppercase tracking-[0.16em] text-stone-300">{link.label}</a>)}</div>}</header>; }

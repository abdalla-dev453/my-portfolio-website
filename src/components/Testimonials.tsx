import { motion } from 'framer-motion';
import { MessageSquareQuote } from 'lucide-react';
import SectionWrapper from './SectionWrapper';

export default function Testimonials() { return <SectionWrapper id="testimonials" className="bg-card"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">Client feedback</p><h2 className="section-title mx-auto">Kind words, when clients approve sharing them.</h2><motion.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="mt-10 border border-line bg-ink p-8"><MessageSquareQuote className="mx-auto h-7 w-7 text-accent" aria-hidden="true" /><p className="mt-5 text-sm leading-7 text-stone-400">Verified client testimonials will be published here with permission. In the meantime, project source code and direct references are available on request.</p><a href="#contact" className="mt-6 inline-flex text-xs font-bold uppercase tracking-[.14em] text-forest hover:text-accent">Request a reference</a></motion.div></div></SectionWrapper>; }

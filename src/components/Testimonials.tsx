import { motion } from 'framer-motion';
import { BadgeCheck, Github, MessageSquareQuote } from 'lucide-react';
import SectionWrapper from './SectionWrapper';

export default function Testimonials() {
  return <SectionWrapper id="testimonials" className="bg-card"><div className="grid gap-10 lg:grid-cols-[.85fr_1.15fr] lg:gap-20"><div><p className="eyebrow">Client feedback</p><h2 className="section-title">Proof should be easy to verify.</h2><p className="mt-7 max-w-md text-sm leading-7 text-stone-400">I only share feedback with permission. Until there is approved client copy to publish, this section points you to the evidence that is available today.</p><a href="#contact" className="mt-8 inline-flex text-xs font-bold uppercase tracking-[.14em] text-forest hover:text-accent">Request a professional reference</a></div><motion.div initial={{ y: 16 }} whileInView={{ y: 0 }} viewport={{ once: true }} className="border border-line bg-ink p-7 sm:p-8"><MessageSquareQuote className="h-7 w-7 text-accent" aria-hidden="true" /><h3 className="mt-8 text-2xl font-bold tracking-[-.035em] text-paper">References, shared responsibly.</h3><p className="mt-4 max-w-xl text-sm leading-7 text-stone-400">Relevant project context, source code, and direct references can be provided during a conversation when appropriate.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="border border-line bg-card p-4"><BadgeCheck className="h-5 w-5 text-accent" aria-hidden="true" /><p className="mt-6 text-sm font-bold text-paper">Clear project context</p><p className="mt-2 text-xs leading-5 text-stone-400">Scope, decisions, and constraints—not just a polished outcome.</p></div><div className="border border-line bg-card p-4"><Github className="h-5 w-5 text-accent" aria-hidden="true" /><p className="mt-6 text-sm font-bold text-paper">Inspectable work</p><p className="mt-2 text-xs leading-5 text-stone-400">Public code and working projects where they can be shared.</p></div></div></motion.div></div></SectionWrapper>;
}
